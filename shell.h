#ifndef SHELL_H
#define SHELL_H

#include <stdio.h>
#include <stdlib.h>

/* memory.c*/
void *_realloc(void *ptr, size_t oldsize, size_t newsize);
void _memcpy(void *new_ptr, void *ptr, size_t size);

#endif
